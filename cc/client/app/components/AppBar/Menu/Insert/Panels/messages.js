import { defineMessages } from "react-intl";

const URL = '@cc/cc/client/app/components/AppBar/Toolbar/Insert/Panel';

export default defineMessages({
    Add: {
        id: `${URL}/Add`,
        defaultMessage: 'Add '
    },
});