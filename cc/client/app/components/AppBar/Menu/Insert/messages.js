import { defineMessages } from "react-intl";

const URL = '@cc/cc/client/app/components/AppBar/Toolbar/Insert';

export default defineMessages({
    Layout: {
        id: `${URL}/Layout`,
        defaultMessage: 'Layout'
    },
    Panel: {
        id: `${URL}/Panel`,
        defaultMessage: 'Panel'
    }
});