import { defineMessages } from "react-intl";

const URL = '@cc/cc/client/app/components/AppBar/MovableMenuList';

export default defineMessages({
    FixPosition: {
        id: `${URL}/FixPosition`,
        defaultMessage: 'Fix position'
    },
    Shuffle: {
        id: `${URL}/Shuffle`,
        defaultMessage: 'Shuffle'
    },
   
});