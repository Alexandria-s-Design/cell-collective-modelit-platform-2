import { defineMessages } from "react-intl";

const URL = '@cc/cc/client/app/components/AppBar/Upgrade';

export default defineMessages({
		Upgrade:{
			id: `${URL}/Upgrade`,
			defaultMessage: 'Upgrade'
		},
		
});