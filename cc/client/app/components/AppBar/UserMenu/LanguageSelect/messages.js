import { defineMessages } from "react-intl";

export default defineMessages({
    ReloadInMoment: {
        id: "AppBar.UserMenu.LanguageSelect.ReloadInMoment",
        defaultMessage: "Application is going to be reloaded in the moment"
    }
});
